interface AdSlotProps {
  slot?: string;
  className?: string;
  format?: 'auto' | 'rectangle' | 'horizontal' | 'vertical';
}

export default function AdSlot({ slot: _slot = '', className: _className = '', format: _format = 'auto' }: AdSlotProps) {
  // Hidden until AdSense is approved and integrated
  return null;
}
